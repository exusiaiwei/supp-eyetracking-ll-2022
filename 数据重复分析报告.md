# 眼动追踪实验数据重复分析报告

## 分析概述

通过对您的实验数据仓库进行详细分析，我发现了一个重要的情况：**414文件夹中的`注视情况.xlsx`文件包含了所有其他Excel文件中的数据，并且是最完整的数据源**。

## 主要发现

### 1. 数据重复情况
- **414文件包含所有参与者数据**：9个参与者 (h1, hxq, ljj, lsx, lx, myw1, wzc, zxj, zy)
- **其他文件数据不完整**：大部分文件只包含部分参与者数据
- **数据量完全匹配**：414文件中的数据量与各个单独文件中的数据量完全一致

### 2. 文件类型分析
- **414文件夹**：包含一个主要的xlsx文件 (`注视情况.xlsx`)，大小315KB，9766行数据
- **其他文件夹**：包含多个.xls文件，实际上是制表符分隔的文本文件，不是真正的Excel文件
- **数据格式**：所有文件都包含相同的列结构（眼动追踪数据）

### 3. 参与者数据分布
| 参与者ID | 新ID | 数据行数 | 说明 |
|---------|------|----------|------|
| h1 | S01 | 1149 | 完整数据 |
| hxq | S02 | 992 | 完整数据 |
| ljj | S03 | 1146 | 完整数据 |
| lsx | S04 | 1089 | 完整数据 |
| lx | S05 | 884 | 完整数据 |
| myw1 | S06 | 1231 | 完整数据 |
| wzc | S07 | 1079 | 完整数据 |
| zxj | S08 | 998 | 完整数据 |
| zy | S09 | 1198 | 完整数据 |

## 建议的数据处理方案

### 方案1：使用414文件作为唯一数据源（推荐）
**优点：**
- 数据最完整，包含所有参与者
- 文件格式统一（xlsx）
- 数据量最大且一致
- 避免了数据重复和不一致的问题

**实施步骤：**
1. 使用`process_414_data.py`脚本从414文件中提取数据
2. 为每个参与者生成单独的CSV文件
3. 统一参与者ID命名（h1→S01, hxq→S02, 等）

### 方案2：继续使用原有的process_data.py
**缺点：**
- 需要处理多个不同格式的文件
- 可能产生重复数据
- 文件数量多（44个文件 vs 9个文件）
- 数据一致性难以保证

## 文件对比

### 原有cleaned_data文件夹
- **文件数量**：44个CSV文件
- **数据来源**：多个不同的Excel文件
- **参与者覆盖**：9个参与者，但数据分散在多个文件中

### 新的cleaned_data_from_414文件夹
- **文件数量**：9个CSV文件
- **数据来源**：单一的414文件
- **参与者覆盖**：9个参与者，每个参与者一个文件

## 结论

**强烈建议使用414文件夹中的`注视情况.xlsx`文件作为唯一的数据源**，原因如下：

1. **数据完整性**：包含了所有参与者的完整数据
2. **数据一致性**：避免了多个文件之间的数据不一致问题
3. **处理效率**：只需要处理一个文件，而不是多个不同格式的文件
4. **维护性**：数据管理更加简单和可靠

## 推荐操作

1. 使用`process_414_data.py`脚本生成新的CSV文件
2. 将`cleaned_data_from_414`文件夹作为主要的数据分享文件夹
3. 可以考虑删除或归档原有的`cleaned_data`文件夹
4. 更新数据文档，说明数据来源为414文件

这样可以确保数据的完整性和一致性，同时简化数据管理流程。